#include <bits/stdc++.h>
#define int long long
using namespace std;

int n, k, ans = 1e18;
int a[(int)5e5 + 10];

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cin >> n >> k;
    for(int i = 1; i <= n; i++)
    	cin >> a[i];
    for(int i = 0; i <= n - 3; i++)
    {
    	int temp = 0;
    	
    }
}
